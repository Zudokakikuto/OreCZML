<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.orekit</groupId>
    <artifactId>oresium-parent</artifactId>
    <version>1.1-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>oresium-parent</name>
    <url>https://gitlab.orekit.org/orekit/oresium</url>
    <inceptionYear>2024</inceptionYear>
    <description>
        Oresium is a bridge library between Orekit and czml-writer (by
        AnalyticalGraphicsInc) libraries. The goal is to display orbital 3D
        vizualisation using CesiumJS.
    </description>


    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.compiler.source>17</project.compiler.source>
        <project.compiler.target>17</project.compiler.target>
        <sonar.projectKey>${env.CI_PROJECT_NAMESPACE}:${env.CI_PROJECT_NAME}</sonar.projectKey>
        <sonar.projectName>${env.CI_PROJECT_TITLE} (${env.CI_PROJECT_NAMESPACE}:${env.CI_PROJECT_NAME})</sonar.projectName>
        <sonar.qualitygate.wait>true</sonar.qualitygate.wait>
        <oresium.orekit.version>12.2</oresium.orekit.version>
        <oresium.maven-compiler-plugin.version>3.13.0</oresium.maven-compiler-plugin.version>
        <oresium.maven-source-plugin.version>3.3.1</oresium.maven-source-plugin.version>
        <oresium.maven-javadoc-plugin.version>3.6.0</oresium.maven-javadoc-plugin.version>
        <oresium.build-helper-maven-plugin.version>3.4.0</oresium.build-helper-maven-plugin.version>
        <oresium.maven-gpg-plugin.version>3.1.0</oresium.maven-gpg-plugin.version>
        <oresium.maven-install-plugin.version>3.1.1</oresium.maven-install-plugin.version>
        <oresium.maven-checkstyle-plugin.version>3.6.0</oresium.maven-checkstyle-plugin.version>
        <!-- Checkstyle 9.3 seems to be the last version usable with JDK 8 -->
        <oresium.checkstyle.version>9.3</oresium.checkstyle.version>
        <oresium.spotless.version>2.44.0</oresium.spotless.version>
    </properties>

    <build>
        <plugins>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${oresium.maven-compiler-plugin.version}</version>
                <configuration>
                    <source>${project.compiler.source}</source>
                    <target>${project.compiler.target}</target>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>${oresium.maven-checkstyle-plugin.version}</version>
                <dependencies>
                    <dependency>
                        <groupId>com.puppycrawl.tools</groupId>
                        <artifactId>checkstyle</artifactId>
                        <version>${oresium.checkstyle.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <configLocation>${basedir}/checkstyle.xml</configLocation>
                    <enableRulesSummary>false</enableRulesSummary>
                    <headerLocation>${basedir}/license-header.txt</headerLocation>
                </configuration>
            </plugin>

            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>${oresium.spotless.version}</version>

                <executions>
                    <execution>
                        <goals>
                            <goal>apply</goal>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>

                <configuration>
                    <java>
                        <eclipse>
                            <file>${project.basedir}/orekit-eclipse-formatter.xml</file>
                        </eclipse>
                        <trimTrailingWhitespace/>
                        <endWithNewline/>
                        <encoding>UTF-8</encoding>
                        <lineEndings>UNIX</lineEndings>
                        <indent>
                            <spaces>true</spaces>
                        </indent>
                        <includes>
                            <include>oresium-core/src/main/java/**/*.java</include>
                            <include>oresium-core/src/test/java/**/*.java</include>
                            <include>oresium-tutorials/src/main/java/**/*.java</include>
                        </includes>
                    </java>
                </configuration>
            </plugin>

        </plugins>
    </build>


    <modules>
        <module>oresium-core</module>
        <module>oresium-tutorials</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <!-- https://mvnrepository.com/artifact/org.orekit/orekit -->
            <dependency>
                <groupId>org.orekit</groupId>
                <artifactId>orekit</artifactId>
                <version>${oresium.orekit.version}</version>
            </dependency>
            <dependency>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>${oresium.spotless.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <distributionManagement>
        <snapshotRepository>
            <id>gitlab-maven</id>
            <url>${env.CI_API_V4_URL}/projects/${env.CI_PROJECT_ID}/packages/maven</url>
        </snapshotRepository>
        <repository>
            <id>gitlab-maven</id>
            <url>${env.CI_API_V4_URL}/projects/${env.CI_PROJECT_ID}/packages/maven</url>
        </repository>
    </distributionManagement>

    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <version>${oresium.maven-source-plugin.version}</version>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <goals>
                                    <goal>jar-no-fork</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <version>${oresium.maven-javadoc-plugin.version}</version>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>build-helper-maven-plugin</artifactId>
                        <version>${oresium.build-helper-maven-plugin.version}</version>
                        <executions>
                            <execution>
                                <id>attach-artifacts</id>
                                <phase>install</phase>
                                <goals>
                                    <goal>attach-artifact</goal>
                                </goals>
                                <configuration>
                                    <artifacts>
                                        <artifact>
                                            <file>${basedir}/target/oresium-${project.version}-sources.jar</file>
                                            <type>source-jar</type>
                                        </artifact>
                                    </artifacts>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>${oresium.maven-gpg-plugin.version}</version>
                        <configuration>
                            <gpgArguments>
                                <arg>--digest-algo=SHA512</arg>
                            </gpgArguments>
                            <keyname>BED6C076F306C70DE6A66A4609CBF7450F49D817</keyname>
                        </configuration>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-install-plugin</artifactId>
                        <version>${oresium.maven-install-plugin.version}</version>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
